# Maintainer: Your Name <youremail@domain.com>
pkgname=quackie
pkgver=0.1.0
pkgrel=1
epoch=
pkgdesc="Rotating duck for your terminal"
arch=('any')
url=""
license=('Unlicense')
groups=()
depends=('ncurses')
makedepends=()
checkdepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=("$pkgname-$pkgver.tar.gz")
sha256sums=('680015ef7ddbfc6befa649e6ffeb5082422285830bcf4804e6d69fc8c6a33c48')

prepare() {
    # No preparation needed
    :
}

build() {
    # No compilation needed
    :
}

check() {
    # No tests provided
    :
}

package() {
	# Install the binary
    install -Dm755 "bin/quackie" "$pkgdir/usr/bin/quackie"
}

